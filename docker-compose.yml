version: '3.8'

services:
  
  app:
    build: .
    volumes:
      - .:/app
    image: dict:django
    container_name: dictapp
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
